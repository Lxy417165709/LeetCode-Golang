## 二维矩阵广度优先搜索 (TODO)
1. 框架
    ``` go
    // 坐标结构体
    type Node struct {
    	x, y int
    }
    
    func BFS(){
        /* 确定搜索方向 */
        /* 将满足要求的坐标节点入队 */
        for /* 队列非空 */ {
            /* 取出队首元素，并删除该队首元素 */
            for /* 遍历所有方向 */ {
                if /* 判断新坐标合法性及是否已入队 */ {
                    /* 入队并标记 */
                }
            }
        }
    }
    
    ```
